#0x12. Web stack debugging #2
This is a Holberton School educational project.

#Concepts explored

- Debugging tools

## Usage

Educational purposes

## Tasks
0. 0-iamsomeonelese: write a Bash script that accepts one argument, the script should run the whoami command under the user passed as an argument, make sure to try your script by passing different users.
1. 1-run_nginx_as_nginx:Script that configurates a container to : nginx must be running as nginx user, nginx must be listening on all active IPs on port 8080, you cannot use apt-get remove.
